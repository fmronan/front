{{ include('VitelogeEstimationBundle:CityStats:Fragment/surface_price.html.twig', {
    statistics: [
        { type: 'Appartement', city: city, date: 'now', price: (barometers['a']|last|default({value:null})).value },
        { type: 'Maison', city: city, date: 'now', price: (barometers['m']|last|default({value:null})).value }
    ]
}) }}
